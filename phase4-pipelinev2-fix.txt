One more pipeline-v2.js swap -- fixes a timing bug I found on verification
============================================================================
Your last upload deployed correctly (I checked the diff on GitHub -- it was
right), but when I tested the live page just now, the app's own router
occasionally wins a race against ours and shows its own old "Client
Pipeline" view instead of the new milestone one, even though the "Pipeline"
nav button stays highlighted. This is timing-dependent so it may have
looked fine when you tried it.

I've made the fix more robust (it now watches for the app overwriting our
content at any time, not just in a fixed window right after you click) and
tested it thoroughly before sending this.

Same drill as before: Add file > Upload files, drag this pipeline-v2.js on
top of the old one, commit to main.

After you commit, wait for it to say "Verified" with a green check on
GitHub before telling me -- that's how we'll know the commit actually took
your new file and not a stale one.
